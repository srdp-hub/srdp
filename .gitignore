# OSX
.DS_Store

# Shell
.zshrc.save

# Environment variables
.env
secrets.sh
.secrets/

# Local certificates and PAT for Zitadel
certs/
login-client.pat

# Zensical build output
site/

# Terraform
.terraform
terraform.tfstate
terraform.tfstate.backup
kubeconfig.yaml

# Kubernetes
values-prod.yaml
charts/

# Local ADR review notes (not committed)
docs/adr/ADR-REVIEW.md
docs/reviews

# Python
__pycache__/
*.pyc
*.pyo
*.pyd

# dbt
target/
dbt_packages/
logs/
.user.yml

# DuckLake local storage default (only used outside Docker/k8s, which mount their own path)
.data/

.agent
.agents

# Claude Code local overrides (personal, machine-specific — .claude/settings.json is shared)
.claude/settings.local.json
